import React from 'react'
import Home from '../components/Home'

const HomePage = ({addTime}) => {
    return (
        <div>
            <Home addTime={addTime}/>
        </div>
    )
}

export default HomePage
